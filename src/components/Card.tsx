import '../output.css';
import { useState } from 'react';

export default function CharCard(props: {propCharType: string}) {
    const [charType, setCharType] = useState('');
    
    
    return(
        <div>
            
        </div>
    )
}